// import api from './API/api_key.js'
// Api RAWG
const DOMAIN = "https://api.rawg.io/api",
    // GAMES
    // GAMES = `${DOMAIN}/games?key=${api.APIKEY}&metacritic=90,${Math.floor(Math.random() * (100 - 91) + 91)}`,
    // GAMES_SEARCH = `${GAMES}&search=`,
    GAME = `${DOMAIN}/games/`

export default {
    // GAMES, GAMES_SEARCH, GAME
}